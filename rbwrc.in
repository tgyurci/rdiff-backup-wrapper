# This is a sample config. Do not use it in a production environment without changing values.

# The base directory where config suites are. This is required.
suitebase=/etc/backup/rbw

# Default age of increments to keep. It can be overridden in suite config.
# See --remove-older-than
max_age="2W"

# Compressed ssh conection or not. It can be overridden in suite config.
# See --ssh-no-compression
compress="NO"

# Verbosity level. 
# See --verbosity
verbose="5"

# Minimum file size
# See --min-file-size
min_size="2"

# Maximum file size
# See --max-file-size
max_size="4294967296"

# These parameters are suite-specific and must not be set in global config.
#sourcehost="srchost"
#sourceuser="srcuser"
#sourcedir="/source/dir"
#targethost="targethost"
#targetuser="targetuser"
#targetdir="/target/dir"

# vim:ft=sh
